Window.SetBackgroundTopColor(0, 0, 0);
Window.SetBackgroundBottomColor(0, 0, 0);

img = Image("logo.png");
img.sprite.SetX(Plymouth.GetWidth() / 2 - img.GetWidth() / 2);
img.sprite.SetY(Plymouth.GetHeight() / 2 - img.GetHeight() / 2);
